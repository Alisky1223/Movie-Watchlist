| # | نوع ایراد (Category) | شدت (Severity) | موقعیت (File/Line) | شرح مشکل (Issue) | راهکار پیشنهادی (Solution) |
|---:|---|---|---|---|---|
| 1 | Architecture / Vertical Slice | Medium | src/Execution/AddMovie/Workflow/AddMovieOrchestrator.cs | Orchestrator در حال انجام هم‌زمان validate/business coordination و ساخت entity است؛ اگر منطق domain بیشتر شود، این کلاس از SRP خارج می‌شود. | منطق ساخت و تبدیل entity را به یک action/domain method اختصاصی منتقل کنید تا orchestrator فقط هماهنگی کند و slice ساختار تمیزتر و قابل تست‌تر بماند. |
| 2 | Architecture / Separation of Concerns | Medium | src/Execution/UpdateMovie/Workflow/UpdateMovieOrchestrator.cs | بررسی تکراری بودن عنوان و اعمال تغییرات domain در همان orchestrator انجام می‌شود؛ این باعث شلوغی منطق و پیچیدگی بیشتر می‌شود. | چک‌چک تکراری بودن عنوان را در action جداگانه و اعمال تغییرات را در یک domain method یا action مجزا پیاده‌سازی کنید تا orchestrator فقط جریان را هماهنگ کند. |
| 3 | Defensive Coding / Null Handling | Medium | src/Execution/UpdateMovie/Workflow/UpdateMovieOrchestrator.cs | استفاده از `result.Value!` و `command.Title?.Trim()` در چند نقطه، اگر در آینده flow result از نوع nullable یا invalid شود، ممکن است به رفتار غیرقابل پیش‌بینی منجر شود. | در همه مسیرهای success، وجود payload را قبل از استفاده با guard یا explicit check تضمین کنید و برای title نیز اگر نیاز به تغییر است، از یک branch شفاف و typed استفاده کنید. |
| 4 | Defensive Coding / Validation | Medium | src/Execution/GetMovies/Delivery/GetMoviesEndpoint.cs | endpoint فقط از page/pageSize fallback استفاده می‌کند اما هیچ validation صریح برای حداقل/حداکثر معتبر وجود ندارد. | قبل از اجرای orchestrator، محدودیت‌های pagination را با validation یا guard clause اعمال کنید تا ورودی‌های نامعتبر به لایه پایین نرسند. |
| 5 | Security / Error Handling | Low | src/Execution/AddMovie/Delivery/AddMovieEndpoint.cs | خطای غیرمنتظره به‌صورت generic ProblemDetails برگردانده می‌شود؛ این موضوع برای audit و observability کافی نیست. | برای خطاهای شناخته‌شده و domain errors، از typed mapping روشن استفاده کنید و برای خطاهای غیرمنتظره، traceId و structured error code را در ساختار خروجی حفظ کنید. |
| 6 | Testing / Coverage | Medium | tests/Execution.Tests/MovieCrudIntegrationTests.cs | تست‌ها روی add/get تمرکز دارند و update/delete را به‌صورت regression end-to-end پوشش نمی‌دهند. | برای پوشش کامل acceptance criteria، تست‌های end-to-end برای update و delete اضافه کنید و رفتار 404/409 را نیز بررسی کنید. |
| 7 | Persistence / Domain Boundary | Medium | src/Persistence/Repositories/MovieRepository.cs | repository در لایه persistence از منطق domain-specific comparison و normalization استفاده می‌کند؛ این می‌تواند در آینده با domain rules drift کند. | منطق مربوط به case-insensitive uniqueness را در domain/service یا invariant مناسب نگه دارید و repository فقط دسترسی به داده را فراهم کند. |
| 8 | API Contract / Consistency | Low | src/Execution/DeleteMovie/Workflow/DeleteMovieOrchestrator.cs | response delete به‌صورت Result.Success بدون payload یا explicit deletion result پیاده‌سازی شده؛ این برای future extensibility کم‌قدرت است. | اگر نیاز به اطلاعات بیشتر در آینده باشد، از یک typed delete result استفاده کنید تا contract endpoint و orchestration روشن‌تر باشد. |
